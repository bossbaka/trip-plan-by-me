---
import NavBar from '../component/Navbar'
import JumboTab from '../component/JumboTab'
import FloatingBubble from '../component/FloatingBubble'

import '../styles/global.css'

import { db, PlanDate } from 'astro:db'
const planDate = await db.select().from(PlanDate)
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro</title>
  </head>
  <body>
    <NavBar />
    <!-- <JumboTab client:load planDate={planDate} /> -->
    <slot />
  </body>
</html>
