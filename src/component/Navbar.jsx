import { NavBar, Space, Toast } from 'antd-mobile'

function NavbarComponent() {
  return (
    <NavBar back={null}>
      <div>
        <h4>My Birthday Trip</h4>
      </div>
    </NavBar>
  )
}

export default NavbarComponent
